using UnityEngine;
using System.Collections;

public class Fireworks : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 46f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
